// SwiftUSD - High-performance Swift interface to Pixar's OpenUSD
//
// This module provides idiomatic Swift wrappers around the CxxFacade C++ layer,
// which in turn wraps the real Pixar USD library.

import CxxFacade

/// SwiftUSD version information
public enum SwiftUSD {
    public static let version = "2.0.0"
}
